# Test assignment

To use the assignment:

- Clone the repository
- Run `npm install`
- Copy `envTemplate` file as `.env`
- Edit the `.env` file and add the API KEY where is marked YOUR_API_KEY
- Run `npm run dev`
- Open `localhost:3000` in any browser
